/* 
Array Methods merupakan method/function bawaan daripada tipe data array tsb.
Ciri2 Array Method yaitu:
    - nama variable yang bertipe array di ikuti tanda titik pada belakang nama variabel tsb dan diikuti oleh keyword dengan tipe function maupun properties
    contoh:
        const a1 = [1,2,3]
        a1.length   >>> ".length" adalah array properties
        a1.join(',') >>> ".join()" adalah array method 
*/

const a1 = ['a', 'b', 'c', 'e', 'd']
console.log(1 + ' > ' + 2 + ' > ' + 3 + ' > ' + true)
console.log(typeof a1.join(' > '))
console.log(a1.pop())
